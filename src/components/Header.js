import React from "react"
import "../assets/sass/main.scss"

export default function Header() {
    return (
        <header>
            <div class="menu">
                <ul>
                </ul>
            </div>
        </header>
    );
}